[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install --no-cache-dir -r server/requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir server"
